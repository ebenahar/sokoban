package Commands;
enum 
public class Move implements Command {

	@Override
	public void execute(int x, int y, int dx, int dy, String trialBoard) {
				 
	        int newPlayerPos = (y + dy) + x + dx;
	 
//implementation
	 
		}
}
